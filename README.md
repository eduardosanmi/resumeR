# resumeR
Interactive resume timeline app in shiny

# Run in R from github:

require(shiny)
library(timevis)
library(data.table)
library(text2vec)
library(wordcloud)
runGitHub("resumeR", "eduardosanmi")

